import 'package:flutter/material.dart';

//TODO: Implement Error view
class ErrorView extends StatelessWidget {
  final String _text;

  const ErrorView(this._text, {Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}